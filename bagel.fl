# Testing superclass method calls.

class Bagel
  cook()
    print "toasting"
    this.finish("nothing")
  
  finish(ingredient)
    print "finished with =(ingredient)"

class PumpkinBagel < Bagel
  finish(ingredient)
    super.finish("cream cheese")

var new = PumpkinBagel()
new.cook()
